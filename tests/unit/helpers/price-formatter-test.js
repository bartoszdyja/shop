
import { priceFormatter } from 'shop-app/helpers/price-formatter';
import { module, test } from 'qunit';

module('Unit | Helper | price formatter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = priceFormatter([42]);
  assert.ok(result);
});

